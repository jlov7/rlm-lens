# Demo prompts for RLM-Lens

1. Summarize the architecture and cite the 2–3 most important files.
2. Where is the retry policy defined? Cite exact line ranges.
3. How is the retry policy used by the API layer? Cite it.
4. What tables exist in the database schema? Cite the SQL.
5. List any TODOs in the repo and group them by folder (if none, say none).

